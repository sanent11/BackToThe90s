BTTS_updateEconomic = {
	hidden_effect = {
			set_variable = {
				var = sum_res
				value = var:ROOT.resource_exported@oil
			}	
			divide_variable = {
				var = sum_res
				value = 3
			}
			add_to_variable = {
				var = sum_res
				value = var:ROOT.resource_exported@steel
			}		
			add_to_variable = {
				var = sum_res
				value = var:ROOT.resource_exported@aluminium
			}						
			
			add_to_variable = {
				var = sum_res
				value = var:ROOT.resource_exported@tungsten
			}		
			add_to_variable = {
				var = sum_res
				value = var:ROOT.resource_exported@chromium
			}			

			add_to_variable = {
				var = sum_res
				value = var:ROOT.resource_exported@uran
			}		
			add_to_variable = {
				var = sum_res
				value = var:ROOT.resource_exported@uran
			}		
			add_to_variable = {
				var = sum_res
				value = var:ROOT.resource_exported@uran
			}
			set_variable = {
				var = sum_res_fabricsget
				value = var:sum_res
			}

			divide_variable = {
				var = sum_res_fabricsget
				value = 10
				tooltip = divide_var_by_100_tt
			}

			add_to_variable = {
				var = economic_IT_progress
				value = var:economic_ITprogress_growth
			}	
			set_variable = {
				var = economic_fabricsget
				value = sum_res_fabricsget
			}	
					
		}
	}
}